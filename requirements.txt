# Local dev convenience install (`pip install -r requirements.txt`).
# Vercel's build reads runtime deps from pyproject.toml's [project.dependencies]
# instead (confirmed 2026-07-26 — see DECISIONS.md: once pyproject.toml exists,
# Vercel ignores this file entirely). Keep the two lists in sync by hand.
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
cryptography>=42.0
matplotlib>=3.9
numpy>=1.26
google-genai>=0.3
psycopg[binary]>=3.1
pyjwt>=2.8

# dev / test only — not needed at runtime, kept here for a single-file install
pytest>=8.2
pytest-cov>=5.0
freezegun>=1.5
